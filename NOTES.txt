NOTES:: 

*Navbar 
    - when click in menu (cell phone), goes to page and close dropdown menu (quando clico em "home", ele vai pra pagina mas nao fecha o menu - fazer fechar)
    - increase font size of hamburguer icon

*ContactUs
    - server (send data of users/message to be visible to owner of page)
    - focus in form (remove black border)
    - adjust media query (phone size)

*User Form (Login, Register)
    - adjust media query (phone size)

*Login
    - make it work (server)
    - if user is owner of the soap store, so render the create/edit soap

*Register
    - make it work (server)

*Home
    - get better look to it

*SoapList 
    - remove/include btn of new/edit soap (only owner can see)

*SoapCard 
    - adjust photo(there is a small gap btween photo and container)
    - add card shadow

*implement REACT REDUX
    - implement Redux to create/edit a soap and in the soap card to have a proper 'id'

*implement Shades in cards/btn













**O que FALTA:
- ever login que nao esta terminado



** o api server é foi configurado com o "JSON server" package


**como abrir o projeto
in "soap-project" run "npm start"
in "backend" run "nodemon server.js"
in "api" run "npm start"